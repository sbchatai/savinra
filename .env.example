# Supabase — use publishable key (not anon) for new apps
VITE_SUPABASE_URL=https://rzknetoapokbwmyhvqac.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_RoaATgu6II9luXOg3A7lTQ_-uQV8gsd

# Razorpay — Key ID only (never expose Key Secret client-side)
VITE_RAZORPAY_KEY_ID=

# Google Places — restrict to domain in Google Cloud Console
VITE_GOOGLE_PLACES_API_KEY=

# Set by Vercel/CI — do not put in .env
# SUPABASE_SERVICE_ROLE_KEY=       ← Edge Functions only
# RAZORPAY_KEY_SECRET=             ← Edge Functions only
# ANTHROPIC_API_KEY=               ← Edge Functions only
# FASHN_AI_API_KEY=                ← Edge Functions / n8n only
# SHIPROCKET_EMAIL=                ← n8n only
# SHIPROCKET_PASSWORD=             ← n8n only
# WHATSAPP_API_KEY=                ← n8n only
